#ifndef SUM_REVERSE_SORT_H
#define SUM_REVERSE_SORT_H
#pragma once
#include <string>
#include <vector>

using namespace std;

int Sum(int x, int y);
string Reverse(string s);
void Sort(vector<int>& nums);


#endif // SUM_REVERSE_SORT_H
